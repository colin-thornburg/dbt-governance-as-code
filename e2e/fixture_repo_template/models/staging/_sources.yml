version: 2

sources:
  - name: raw
    description: "Raw source tables used by the fixture project"
    schema: raw
    tables:
      - name: customers
        description: "Raw customer records"
      - name: accounts
        description: "Raw account records"
